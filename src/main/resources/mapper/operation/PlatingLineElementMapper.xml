<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.payment.mapper.operation.PlatingLineElementMapper">
    <select id="findCurrentValuesByAssetCode"
            parameterType="string"
            resultType="org.payment.domain.operation.PlatingLineElementRow">
        SELECT
            plating_element_type_index AS platingElementTypeIndex,
            plating_now_value AS platingNowValue
        FROM plating_line_element
        WHERE `assetCode` = #{assetCode}
          AND plating_element_type_index IN (3, 4)
    </select>
</mapper>
